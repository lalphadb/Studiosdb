/* =============================================
   STUDIOS UNIS - NÉOMORPHISME DARK ELITE
   Design sculptural avec reliefs organiques
   ============================================= */

:root {
    /* PALETTE NÉOMORPHISME */
    --primary-bg: #1a1a1a;
    --surface-bg: #252525;
    --elevated-bg: #2f2f2f;
    --sunken-bg: #1f1f1f;
    
    /* COULEURS ORGANIQUES */
    --accent-gold: #d4af37;
    --warm-copper: #b87333;
    --steel-blue: #4682b4;
    --sage-green: #87a96b;
    --warm-gray: #8b8680;
    
    /* OMBRES NÉOMORPHIQUES */
    --shadow-raised: 
        6px 6px 12px rgba(0, 0, 0, 0.4),
        -6px -6px 12px rgba(255, 255, 255, 0.05);
    --shadow-pressed: 
        inset 4px 4px 8px rgba(0, 0, 0, 0.5),
        inset -4px -4px 8px rgba(255, 255, 255, 0.03);
    --shadow-floating: 
        0 10px 30px rgba(0, 0, 0, 0.6),
        0 1px 8px rgba(255, 255, 255, 0.1);
    
    /* TEXTE RAFFINÉ */
    --text-primary: #e8e8e8;
    --text-secondary: #b8b8b8;
    --text-muted: #888888;
    --text-accent: var(--accent-gold);
    
    /* TRANSITIONS ORGANIQUES */
    --transition-smooth: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    --transition-bounce: all 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'SF Pro Display', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--primary-bg);
    color: var(--text-primary);
    line-height: 1.6;
    font-size: 15px;
    -webkit-font-smoothing: antialiased;
}

body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
        radial-gradient(circle at 30% 20%, rgba(212, 175, 55, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 70% 80%, rgba(70, 130, 180, 0.06) 0%, transparent 50%);
    z-index: -1;
    animation: organicFlow 25s ease-in-out infinite;
}

@keyframes organicFlow {
    0%, 100% { 
        filter: hue-rotate(0deg);
        transform: scale(1) rotate(0deg);
    }
    50% { 
        filter: hue-rotate(15deg);
        transform: scale(1.05) rotate(1deg);
    }
}

.studios-wrapper {
    display: flex;
    min-height: 100vh;
}

.studios-main {
    flex: 1;
    margin-left: 280px;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

/* SIDEBAR SCULPTÉE */
.studios-sidebar {
    width: 280px;
    background: var(--surface-bg);
    position: fixed;
    height: 100vh;
    left: 0;
    top: 0;
    z-index: 1000;
    display: flex;
    flex-direction: column;
    box-shadow: var(--shadow-raised);
    overflow-y: auto;
    border-right: 1px solid rgba(255, 255, 255, 0.05);
}

.studios-brand {
    padding: 32px 24px;
    background: var(--elevated-bg);
    margin: 20px;
    border-radius: 24px;
    box-shadow: var(--shadow-raised);
    display: flex;
    align-items: center;
    gap: 16px;
    transition: var(--transition-smooth);
}

.studios-brand:hover {
    box-shadow: var(--shadow-floating);
    transform: translateY(-2px);
}

.brand-logo {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, var(--accent-gold), var(--warm-copper));
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    color: white;
    box-shadow: var(--shadow-pressed);
    transition: var(--transition-bounce);
}

.studios-brand:hover .brand-logo {
    transform: scale(1.05);
    box-shadow: 
        inset 2px 2px 4px rgba(0, 0, 0, 0.3),
        inset -2px -2px 4px rgba(255, 255, 255, 0.1);
}

.brand-title {
    font-size: 20px;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0;
    letter-spacing: -0.02em;
}

.studios-user-panel {
    padding: 24px;
    margin: 0 20px 20px;
    background: var(--sunken-bg);
    border-radius: 20px;
    box-shadow: var(--shadow-pressed);
    transition: var(--transition-smooth);
}

.studios-user-panel:hover {
    background: var(--surface-bg);
    box-shadow: var(--shadow-raised);
}

.user-avatar {
    width: 52px;
    height: 52px;
    background: linear-gradient(135deg, var(--steel-blue), var(--sage-green));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    color: white;
    margin-bottom: 12px;
    box-shadow: var(--shadow-raised);
    transition: var(--transition-bounce);
}

.studios-user-panel:hover .user-avatar {
    transform: scale(1.08);
    box-shadow: var(--shadow-floating);
}

.user-name {
    font-size: 16px;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 4px;
}

.user-role {
    font-size: 13px;
    color: var(--text-accent);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    font-weight: 500;
}

.studios-nav {
    flex: 1;
    padding: 20px 0;
}

.nav-list {
    list-style: none;
    padding: 0;
    margin: 0;
}

.nav-item {
    margin-bottom: 6px;
}

.nav-link {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 16px 24px;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 16px;
    margin: 0 20px;
    transition: var(--transition-smooth);
    font-weight: 500;
    background: var(--surface-bg);
    box-shadow: var(--shadow-pressed);
}

.nav-link:hover {
    color: var(--text-primary);
    background: var(--elevated-bg);
    text-decoration: none;
    transform: translateX(4px);
    box-shadow: var(--shadow-raised);
}

.nav-link.active {
    color: var(--text-accent);
    background: var(--elevated-bg);
    box-shadow: var(--shadow-raised);
    transform: translateX(4px);
}

.nav-link i {
    width: 20px;
    text-align: center;
    font-size: 18px;
    transition: var(--transition-smooth);
}

.nav-link:hover i,
.nav-link.active i {
    transform: scale(1.1);
    color: var(--accent-gold);
}

.nav-badge {
    background: linear-gradient(135deg, #ff4757, #ff3838);
    color: white;
    font-size: 11px;
    padding: 4px 10px;
    border-radius: 12px;
    margin-left: auto;
    font-weight: 600;
    box-shadow: var(--shadow-raised);
}

.logout-btn {
    width: calc(100% - 40px);
    margin: 20px;
    padding: 16px 24px;
    background: var(--sunken-bg);
    border: 1px solid rgba(255, 75, 87, 0.3);
    color: #ff4757;
    border-radius: 16px;
    cursor: pointer;
    transition: var(--transition-smooth);
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 14px;
    font-weight: 600;
    box-shadow: var(--shadow-pressed);
}

.logout-btn:hover {
    background: #ff4757;
    color: white;
    border-color: #ff4757;
    box-shadow: var(--shadow-raised);
    transform: translateY(-2px);
}

/* HEADER ORGANIQUE */
.studios-header {
    background: var(--surface-bg);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    padding: 24px 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: var(--shadow-raised);
    position: sticky;
    top: 0;
    z-index: 999;
}

.page-title {
    display: flex;
    align-items: center;
    gap: 16px;
    font-size: 28px;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0;
}

.page-title i {
    width: 52px;
    height: 52px;
    background: linear-gradient(135deg, var(--accent-gold), var(--warm-copper));
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    color: white;
    box-shadow: var(--shadow-raised);
    transition: var(--transition-bounce);
}

.page-title:hover i {
    transform: scale(1.05) rotate(5deg);
}

.header-info {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 8px;
}

.header-date {
    font-size: 14px;
    color: var(--text-secondary);
    font-weight: 500;
}

.header-time {
    background: var(--sunken-bg);
    padding: 10px 16px;
    border-radius: 12px;
    color: var(--text-accent);
    font-size: 14px;
    font-weight: 600;
    font-family: 'SF Mono', monospace;
    box-shadow: var(--shadow-pressed);
}

.studios-content {
    flex: 1;
    padding: 40px;
    background: var(--primary-bg);
}

/* CARTES SCULPTÉES */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 32px;
    margin-bottom: 48px;
}

.stat-card-ultimate {
    background: var(--surface-bg);
    border-radius: 24px;
    padding: 32px;
    transition: var(--transition-smooth);
    box-shadow: var(--shadow-raised);
    cursor: pointer;
    position: relative;
    overflow: hidden;
}

.stat-card-ultimate::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--accent-gold), var(--warm-copper));
    border-radius: 24px 24px 0 0;
    opacity: 0;
    transition: var(--transition-smooth);
}

.stat-card-ultimate:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-floating);
}

.stat-card-ultimate:hover::before {
    opacity: 1;
}

.card-header-ultimate {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 24px;
}

.card-icon-ultimate {
    width: 64px;
    height: 64px;
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    color: white;
    transition: var(--transition-bounce);
    box-shadow: var(--shadow-raised);
}

.icon-primary-ultimate {
    background: linear-gradient(135deg, var(--steel-blue), #5a9fd4);
}

.icon-success-ultimate {
    background: linear-gradient(135deg, var(--sage-green), #a2b86c);
}

.icon-warning-ultimate {
    background: linear-gradient(135deg, var(--warm-copper), #cd853f);
}

.icon-danger-ultimate {
    background: linear-gradient(135deg, #ff6b7d, #ee5a6f);
}

.stat-card-ultimate:hover .card-icon-ultimate {
    transform: scale(1.1) rotate(5deg);
    box-shadow: var(--shadow-floating);
}

.card-content-ultimate {
    flex: 1;
    margin-left: 20px;
}

.card-number-ultimate {
    font-size: 36px;
    font-weight: 800;
    color: var(--text-primary);
    margin: 0 0 8px 0;
    line-height: 1;
}

.card-label-ultimate {
    color: var(--text-secondary);
    font-size: 15px;
    font-weight: 500;
    margin: 0;
}

.card-footer-ultimate {
    margin-top: 24px;
    padding-top: 20px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
}

.card-link-ultimate {
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: var(--text-muted);
    text-decoration: none;
    font-weight: 500;
    font-size: 14px;
    transition: var(--transition-smooth);
}

.card-link-ultimate:hover {
    color: var(--text-accent);
    text-decoration: none;
    transform: translateX(4px);
}

/* SECTIONS ORGANIQUES */
.content-section-ultimate {
    background: var(--surface-bg);
    border-radius: 24px;
    padding: 40px;
    margin-bottom: 32px;
    box-shadow: var(--shadow-raised);
    transition: var(--transition-smooth);
}

.content-section-ultimate:hover {
    box-shadow: var(--shadow-floating);
    transform: translateY(-2px);
}

.section-title-ultimate {
    display: flex;
    align-items: center;
    color: var(--text-primary);
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 32px;
    padding-bottom: 20px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}

.section-title-ultimate i {
    margin-right: 16px;
    width: 44px;
    height: 44px;
    background: linear-gradient(135deg, var(--accent-gold), var(--warm-copper));
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    color: white;
    box-shadow: var(--shadow-raised);
}

/* BOUTONS ORGANIQUES */
.btn-glass-ultimate {
    padding: 14px 28px;
    background: linear-gradient(135deg, var(--accent-gold), var(--warm-copper));
    border: none;
    border-radius: 14px;
    color: white;
    font-weight: 600;
    font-size: 14px;
    cursor: pointer;
    transition: var(--transition-smooth);
    display: inline-flex;
    align-items: center;
    gap: 8px;
    text-decoration: none;
    box-shadow: var(--shadow-raised);
}

.btn-glass-ultimate:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-floating);
    color: white;
    text-decoration: none;
}

.btn-success-ultimate {
    background: linear-gradient(135deg, var(--sage-green), #a2b86c);
}

.btn-warning-ultimate {
    background: linear-gradient(135deg, var(--warm-copper), #cd853f);
}

.btn-danger-ultimate {
    background: linear-gradient(135deg, #ff6b7d, #ee5a6f);
}

/* FORMULAIRES ORGANIQUES */
.form-label {
    display: block;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 8px;
    font-size: 14px;
}

.form-control {
    width: 100%;
    padding: 16px 20px;
    background: var(--sunken-bg);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    color: var(--text-primary);
    font-size: 14px;
    transition: var(--transition-smooth);
    box-shadow: var(--shadow-pressed);
}

.form-control:focus {
    outline: none;
    border-color: var(--accent-gold);
    background: var(--surface-bg);
    box-shadow: var(--shadow-raised);
    color: var(--text-primary);
    transform: translateY(-1px);
}

/* ALERTES ORGANIQUES */
.alert-glass-ultimate {
    padding: 20px 24px;
    border-radius: 16px;
    margin-bottom: 24px;
    display: flex;
    align-items: center;
    gap: 16px;
    font-weight: 500;
    box-shadow: var(--shadow-raised);
}

.alert-success-glass {
    background: var(--surface-bg);
    border-left: 4px solid var(--sage-green);
    color: var(--sage-green);
}

.alert-danger-glass {
    background: var(--surface-bg);
    border-left: 4px solid #ff6b7d;
    color: #ff6b7d;
}

.alert-warning-glass {
    background: var(--surface-bg);
    border-left: 4px solid var(--warm-copper);
    color: var(--warm-copper);
}

.alert-info-glass {
    background: var(--surface-bg);
    border-left: 4px solid var(--steel-blue);
    color: var(--steel-blue);
}

/* TABLEAUX ORGANIQUES */
.table {
    background: var(--surface-bg);
    border-radius: 20px;
    overflow: hidden;
    box-shadow: var(--shadow-raised);
    border: 1px solid rgba(255, 255, 255, 0.05);
}

.table thead th {
    background: var(--elevated-bg);
    font-weight: 600;
    color: var(--text-primary);
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    padding: 20px 16px;
}

.table tbody td {
    color: var(--text-secondary);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    padding: 20px 16px;
    vertical-align: middle;
    transition: var(--transition-smooth);
}

.table tbody tr:hover {
    background: var(--elevated-bg);
}

.table tbody tr:hover td {
    color: var(--text-primary);
}

/* BADGES ORGANIQUES */
.badge {
    padding: 8px 16px;
    border-radius: 12px;
    font-weight: 600;
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    box-shadow: var(--shadow-raised);
}

.bg-success {
    background: linear-gradient(135deg, var(--sage-green), #a2b86c) !important;
    color: white !important;
}

.bg-warning {
    background: linear-gradient(135deg, var(--warm-copper), #cd853f) !important;
    color: white !important;
}

.bg-info {
    background: linear-gradient(135deg, var(--steel-blue), #5a9fd4) !important;
    color: white !important;
}

.bg-primary {
    background: linear-gradient(135deg, var(--accent-gold), var(--warm-copper)) !important;
    color: white !important;
}

/* RESPONSIVE */
@media (max-width: 992px) {
    .studios-main {
        margin-left: 0;
    }
    
    .studios-sidebar {
        transform: translateX(-100%);
        transition: transform 0.3s ease;
    }
    
    .studios-sidebar.open {
        transform: translateX(0);
    }
}

@media (max-width: 768px) {
    .stats-grid {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .studios-content {
        padding: 24px;
    }
    
    .content-section-ultimate {
        padding: 24px;
    }
}

/* SCROLLBARS ORGANIQUES */
::-webkit-scrollbar {
    width: 8px;
}

::-webkit-scrollbar-track {
    background: var(--sunken-bg);
    border-radius: 4px;
}

::-webkit-scrollbar-thumb {
    background: linear-gradient(135deg, var(--accent-gold), var(--warm-copper));
    border-radius: 4px;
    box-shadow: var(--shadow-raised);
}

::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(135deg, var(--warm-copper), var(--accent-gold));
}

 /* Section login */

/* === STYLES D'AUTHENTIFICATION === */
.auth-container {
    min-height: 100vh;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    position: relative;
    overflow: hidden;
}

.auth-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="75" cy="75" r="1" fill="rgba(255,255,255,0.05)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
    pointer-events: none;
}

.auth-card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 20px;
    padding: 2.5rem;
    width: 100%;
    max-width: 450px;
    box-shadow: 
        0 25px 50px rgba(0, 0, 0, 0.1),
        0 0 0 1px rgba(255, 255, 255, 0.05);
    position: relative;
    z-index: 1;
}

.auth-header {
    text-align: center;
    margin-bottom: 2rem;
}

.auth-logo {
    font-size: 3rem;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, #ffd700, #ff8c00);
    width: 80px;
    height: 80px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1rem;
    box-shadow: 0 10px 25px rgba(255, 215, 0, 0.3);
}

.auth-title {
    color: white;
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
}

.auth-subtitle {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.95rem;
    margin-bottom: 0;
}

/* Formulaires */
.form-group-stack {
    margin-bottom: 1.5rem;
}

.form-label {
    color: rgba(255, 255, 255, 0.9);
    font-weight: 600;
    margin-bottom: 0.5rem;
    display: block;
    font-size: 0.9rem;
}

.form-control-stack {
    width: 100%;
    padding: 0.875rem 1rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 12px;
    color: white;
    font-size: 1rem;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
}

.form-control-stack:focus {
    outline: none;
    border-color: rgba(255, 215, 0, 0.6);
    background: rgba(255, 255, 255, 0.15);
    box-shadow: 0 0 0 3px rgba(255, 215, 0, 0.1);
}

.form-control-stack::placeholder {
    color: rgba(255, 255, 255, 0.6);
}

/* Options de connexion */
.form-options {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
}

.checkbox-wrapper {
    display: flex;
    align-items: center;
}

.checkbox-custom {
    width: 18px;
    height: 18px;
    margin-right: 0.5rem;
    accent-color: #ffd700;
}

.checkbox-label {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.9rem;
    margin: 0;
}

.forgot-link {
    color: rgba(255, 215, 0, 0.9);
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.3s ease;
}

.forgot-link:hover {
    color: #ffd700;
    text-decoration: none;
}

/* Boutons */
.btn-primary-stack {
    width: 100%;
    padding: 0.875rem;
    background: linear-gradient(135deg, #ffd700, #ff8c00);
    border: none;
    border-radius: 12px;
    color: #1a1a1a;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
}

.btn-primary-stack:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(255, 215, 0, 0.3);
}

.btn-secondary-stack {
    flex: 1;
    padding: 0.7rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 10px;
    color: rgba(255, 255, 255, 0.9);
    text-decoration: none;
    font-weight: 500;
    text-align: center;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9rem;
}

.btn-secondary-stack:hover {
    background: rgba(255, 255, 255, 0.15);
    color: white;
    text-decoration: none;
    transform: translateY(-1px);
}

.btn-group-stack {
    display: flex;
    gap: 1rem;
}

.btn-danger-stack {
    border-color: rgba(220, 53, 69, 0.3);
    background: rgba(220, 53, 69, 0.1);
}

.btn-danger-stack:hover {
    background: rgba(220, 53, 69, 0.2);
    border-color: rgba(220, 53, 69, 0.5);
}

/* Messages d'erreur */
.error-message {
    color: #ff6b6b;
    font-size: 0.85rem;
    margin-top: 0.5rem;
    display: flex;
    align-items: center;
}

.alert-modern {
    background: rgba(52, 144, 220, 0.1);
    border: 1px solid rgba(52, 144, 220, 0.3);
    border-radius: 10px;
    padding: 1rem;
    color: rgba(52, 144, 220, 0.9);
    margin-bottom: 1.5rem;
    backdrop-filter: blur(10px);
}

/* Politique */
.policy-wrapper {
    margin-bottom: 1.5rem;
}

.policy-checkbox {
    display: flex;
    align-items: flex-start;
}

.policy-text {
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.85rem;
    line-height: 1.4;
    margin: 0;
    margin-left: 0.5rem;
}

.policy-link {
    color: #ffd700;
    text-decoration: underline;
}

.policy-link:hover {
    color: #ffed4e;
}

/* Footer */
.auth-footer {
    text-align: center;
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.85rem;
}

.footer-link {
    color: rgba(255, 255, 255, 0.7);
    text-decoration: none;
}

.footer-link:hover {
    color: white;
    text-decoration: underline;
}

/* Responsive */
@media (max-width: 768px) {
    .auth-card {
        padding: 2rem;
        margin: 1rem;
    }
    
    .auth-title {
        font-size: 1.5rem;
    }
    
    .btn-group-stack {
        flex-direction: column;
    }
}

